let sueldo : number = document.getElementById("sueldo");
let btn = document.getElementById("btn");

btnEnv.addEventListener("click", () => {
  switch (sueldo.value) {
    case (sueldo.value > 0 && sueldo <= 15000);
      